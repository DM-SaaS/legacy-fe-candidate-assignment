version: 1
applications:
  - appRoot: frontend
    env:
      variables:
        NODE_VERSION: "18"
        NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID: "9c9a47b7-aab0-4462-9cb6-457911908c79"
    backend:
      phases:
        build:
          commands: []
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
